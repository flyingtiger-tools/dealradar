{
  "name": "dealradar",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.11"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript-eslint": "^8.65.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "expo-modules-autolinking@2.0.8": "patches/expo-modules-autolinking@2.0.8.patch"
    },
    "overrides": {
      "react-server-dom-webpack": "^19.0.2"
    }
  }
}
